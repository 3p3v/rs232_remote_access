#pragma once

#include <Basic_controller.hpp>

namespace Mqtt_port
{
    /* Lets user to "upload" his own callbacks */
    // template <typename Write_handle_t, typename Conn_handle_t, typename Callb_impl>
    // class Inter_controller : public Basic_controller<Write_handle_t, Conn_handle_t>, public Callb_impl
    // {

    // };
}